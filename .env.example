# Optional public live-application overrides. These values are included in the
# browser bundle, so never use them for credentials, tokens, or private URLs.
# Leave them empty to use the reviewed fallback URLs in the portfolio content.
NEXT_PUBLIC_CHOCOLATE_WEB_APP_URL=
NEXT_PUBLIC_PETNEST_WEB_APP_URL=
